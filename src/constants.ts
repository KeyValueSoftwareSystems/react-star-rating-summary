export enum RatingIds {
	one = 'one',
	two = 'two',
	three = 'three',
	four = 'four',
	five = 'five'
}

export enum Elements {
	Chart = "Chart",
	Count="Count"
}
export const COMMA_SEPARATED_NUMBER_REGEX = /\B(?=(\d{3})+(?!\d))/g;